# .github/workflows/main.yml
name: Main Pipeline
on: [push]

jobs:
  call-build:
    uses: driveops-surya/common-workflow/.github/workflows/reusable-build.yml
    with:
      node-version: '16'
    secrets:
      npm_token: ${{ secrets.NPM_TOKEN }}
